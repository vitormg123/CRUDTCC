<% /* Página de Perfil do Admin */ %>
<div class="perfil-container">
  <div class="perfil-header">
    <% if (usuario.fotoPerfil) { %>
      <img src="<%= usuario.fotoPerfil %>" alt="Foto de Perfil" class="perfil-foto">
    <% } else { %>
      <img src="/img/user.png" alt="Foto de Perfil" class="perfil-foto">
    <% } %>
    <h2><%= usuario.nome %> <span style="font-size:0.7em; color:#b7410e;">(Admin)</span></h2>
  </div>

  <h3>Categorias Cadastradas</h3>
  <ul class="lista-categorias">
    <% if (categorias.length > 0) { %>
      <% categorias.forEach(cat => { %>
        <li>
          <span><%= cat.nome %></span>
          <form action="/categorias/lista/<%= cat.id %>" method="post" style="display:inline; float:right;">
            <button type="submit" class="btn-apagar">Apagar</button>
          </form>
        </li>
      <% }) %>
    <% } else { %>
      <li>Nenhuma categoria cadastrada.</li>
    <% } %>
  </ul>

  <hr>

  <h3>Meu Carrinho</h3>
  <table>
    <tr><th>Produto</th><th>Preço</th><th>Desconto</th><th>Quantidade</th><th>Subtotal</th><th>Ações</th></tr>
    <% if (itens.length > 0) { %>
      <% itens.forEach(item => { %>
        <tr>
          <td><%= item.produto.nome %></td>
          <td>R$ <%= item.produto.preco.toFixed(2) %></td>
          <td><%= item.produto.desconto %> %</td>
          <td><%= item.quantidade %></td>
          <td>R$ <%= (item.produto.preco * item.quantidade * (1 - item.produto.desconto/100)).toFixed(2) %></td>
          <td>
            <form action="/carrinho/remover/<%= item.produto.id %>" method="post" style="display:inline;">
              <button type="submit">Remover</button>
            </form>
          </td>
        </tr>
      <% }) %>
    <% } else { %>
      <tr><td colspan="6">Seu carrinho está vazio.</td></tr>
    <% } %>
  </table>

  <% if (itens.length > 0) { %>
    <p><strong>Total: R$ <%= total.toFixed(2) %></strong></p>
    <form action="/carrinho/finalizar" method="post">
      <button type="submit">Finalizar Compra</button>
    </form>
  <% } %>
</div>
<!-- Zona Perigosa -->
  <div class="danger-zone-wrapper">
    <button class="danger-toggle" onclick="toggleDangerZone()">&gt;</button>
    <div class="danger-zone" id="dangerZone">
      <h4>Zona Perigosa</h4>
      <form action="/usuarios/<%= usuario.usuarioId || usuario.id %>/deletar" method="post" onsubmit="return confirm('Tem certeza que deseja deletar sua conta? Esta ação não pode ser desfeita!');">
  <button type="submit" class="danger-btn">Deletar Conta</button>
      </form>
      <a href="/logout"><button class="danger-btn danger-btn-out">Sair</button></a>
    </div>
  </div>
</div>
<style>
body {
  background: #292929; /* Fundo cinza escuro da página */
}

.perfil-container {
  background: #222; /* Fundo do container */
  border: 2px solid #cf5209; /* Borda laranja */
  border-radius: 8px;
  padding: 30px;
  max-width: 600px;
  margin: 40px auto;
  color: #fff;
}

.perfil-header {
  display: flex;
  align-items: center;
  gap: 20px;
}

.perfil-foto {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  background: #444;
  border: 2px solid #b7410e;
  object-fit: cover;
}

.lista-categorias {
  list-style: none;
  padding: 0;
  margin: 0 0 20px 0;
}

.lista-categorias li {
  background: #333;
  color: #fff;
  margin-bottom: 6px;
  padding: 8px 12px;
  border-radius: 4px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.btn-apagar {
  background: #b7410e;
  color: #fff;
  border: none;
  padding: 6px 12px;
  border-radius: 4px;
  cursor: pointer;
  transition: background 0.2s;
}

.btn-apagar:hover {
  background: #a63a0d;
}
</style>
