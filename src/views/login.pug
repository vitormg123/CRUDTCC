doctype html
html(lang="pt-br")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Entrar - Heranças do Sul
    link(rel="stylesheet", href="/css/style.css")

  body
    .form-bg
      // Container preto da logo
      .logo-container
        img(src="/img/logo.png", alt="Logo Heranças do Sul", class="logo-login")

      .form-box
        h2 Login

        if typeof erro !== 'undefined'
          p(style="color:red")= erro

        form(action="/login", method="post")
          label
            | Email:
          input(type="email", name="email", required)
          label
            | Senha:
          input(type="password", name="senha", required)

          // Container flex para os botões
          .form-buttons(style="display: flex; justify-content: space-between; align-items: center; margin-top: 10px;")
            a(href="/")
              button(type="button", style="background-color: #b7410e; color: #fff; padding: 12px 24px; border: none; border-radius: 5px; cursor: pointer;") Voltar
            button(type="submit").btn Entrar

          p(style="margin-top: 16px; text-align: center; color: #ccc;")
            | Já possui uma conta? 
            a(href="/usuarios/novo", style="color: #b7410e; margin-left: 4px; text-decoration: none; font-weight: bold;") Cadastrar-se

    style.
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
        background: #1a1a1a;
      }

      .form-bg {
        min-height: 100vh;
        width: 100vw;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        background: #1a1a1a;
      }

      .logo-container {
        background: #000;
        border-top-left-radius: 10px;
        border-top-right-radius: 10px;
        border-bottom: none;
        width: 100%;
        max-width: 400px;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 16px 0;
      }

      .logo-login {
        width: 120px;
      }

      .form-box {
        background: #222;
        border: 2.5px solid #b7410e;
        border-top: none;
        border-radius: 0 0 10px 10px;
        padding: 40px 32px 32px 32px;
        box-shadow: 0 2px 16px #0005;
        color: #fff;
        display: flex;
        flex-direction: column;
        align-items: center;
        width: 100%;
        max-width: 400px;
        box-sizing: border-box;
      }

      .form-box h2 {
        margin-bottom: 24px;
        color: #b7410e;
      }

      .form-box label {
        display: block;
        margin-bottom: 16px;
        font-size: 1.05em;
      }

      .form-box input {
        width: 100%;
        padding: 8px;
        margin-top: 4px;
        border-radius: 4px;
        border: 1px solid #444;
        background: #333;
        color: #fff;
        font-size: 1em;
        box-sizing: border-box;
      }

      .btn {
        padding: 12px 24px;
        font-size: 1.1em;
        border: none;
        border-radius: 5px;
        background: #b7410e;
        color: #fff;
        cursor: pointer;
      }
      .btn:hover {
        background: #99330c;
      }
