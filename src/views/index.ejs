<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Heranças do Sul</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <div class="header-content">
      <img src="/img/logo.png" alt="Logo Heranças do Sul" class="logo-header">
      <div class="search-bar">
        <input type="text" placeholder="Pesquisar produtos...">
        <button><img src="/img/search.png" alt="Pesquisar" style="height:20px;"></button>
      </div>
      <nav class="header-nav">
        <a href="/carrinho" style="display:inline-block;"><button><img src="/img/cart.png" alt="Carrinho" style="height:20px;"></button></a>
        <% if (!usuario || !usuario.usuarioId || !usuario.tipo) { %>
          <% if (typeof maisDeUmUsuario !== 'undefined' && maisDeUmUsuario) { %>
            <a href="/login"><button>Entrar</button></a>
          <% } else { %>
            <a href="/usuarios/novo"><button><img src="/img/user.png" alt="Usuário" style="height:20px;vertical-align:middle;margin-right:6px;"></button></a>
          <% } %>
  <% } else { %>
          <% if (usuario.tipo === 'admin') { %>
            <a href="/perfil-admin"><button class="perfil-btn">
              <% if (usuario.fotoPerfil) { %>
                <img src="<%= usuario.fotoPerfil %>" alt="Perfil" class="perfil-icon">
              <% } else { %>
                <img src="/img/user.png" alt="Perfil" class="perfil-icon">
              <% } %>
            </button></a>
          <% } else { %>
            <a href="/perfil"><button class="perfil-btn">
            <% if (usuario.fotoPerfil) { %>
              <img src="<%= usuario.fotoPerfil %>" alt="Perfil" class="perfil-icon">
            <% } else { %>
              <img src="/img/user.png" alt="Perfil" class="perfil-icon">
            <% } %>
            </button></a>
          <% } %>
          <% if (usuario.tipo === 'admin') { %>
            <a href="/categorias/nova"><button style="background:#b7410e;color:#fff;">Cadastrar Categoria</button></a>
            <a href="/produtos/novo"><button style="background:#b7410e;color:#fff;">Cadastrar Produto</button></a>
            <a href="/categorias"><button style="background:#b7410e;color:#fff;">Ver Categorias</button></a>
            <a href="/produtos"><button style="background:#b7410e;color:#fff;">Ver Produtos</button></a>
          <% } %>
        <% } %>
      </nav>
    </div>
  </header>
  <nav class="main-nav">
    <a href="/categorias"><button>Categorias</button></a>
    <a href="/produtos/novidades"><button>Novidades</button></a>
    <a href="/produtos/descontos"><button>Descontos</button></a>
    <a href="/produtos/populares"><button>Populares</button></a>
  </nav>
  <main>
    <h1>Bem-vindo à Heranças do Sul</h1>
    <p>Seu e-commerce de botas!</p>
    <!-- Produtos e filtros serão exibidos aqui -->
  </main>
</body>
</html>
